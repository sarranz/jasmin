export
fn slli(reg u32 arg0) {
    reg u32 x;
    x = #SLLI(arg0, 0);
    x = #SLLI(x, 1);
    x = #SLLI(x, 31);
    // The shift amount is a u8 literal, so it follows ordinary Jasmin
    // sized-literal semantics: 256 wraps modulo 256 to 0, a valid shift,
    // before the "[0, 32)" range check runs.
    x = #SLLI(x, 256);
    [#aligned x] = x;
}

