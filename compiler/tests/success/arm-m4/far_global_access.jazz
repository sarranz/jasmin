param int L = 2042; // works for 2041, fails for 2042
u32[1] H = {1};

export fn main() -> reg u32
{
    inline int i;
    reg ptr u32[1] Hp;
    reg u32 t v;

    Hp = H;
    t = Hp[0];

    v = 0;
    for i=0 to L
              { v += t; }

    return v;
}
