u8[10] test = "Teststring";

export
fn main() -> reg u32 {
    reg u32 res tmp;
    res = 0x54;

    reg ptr u32[2] p;
    p = test[0:8];

    tmp = p[#aligned 0];
    res ^= tmp;
    tmp = p[#aligned 1];
    res ^= tmp;

    return res;
}
